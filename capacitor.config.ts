import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.calculadorapp7',
  appName: 'calculadorapp7',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
